<%= form_for(@step) do |f| %>
  <% if @step.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@step.errors.count, "error") %> prohibited this step from being saved:</h2>

      <ul>
      <% @step.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :mobID %><br />
    <%= f.text_field :mobID %>
  </div>
  <div class="field">
    <%= f.label :longline %><br />
    <%= f.text_field :longline %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
